#ifndef __PARALLEL_HANDLE__
    #define __PARALLEL_HANDLE__

    // -- LOCAL

    // .. REFERENCES

    // -- GLOBAL

    // .. TYPES

    typedef HANDLE
        PARALLEL_HANDLE;
#endif
